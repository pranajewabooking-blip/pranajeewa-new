name: Manual Render API Health Check

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API health endpoint
        run: |
          curl --fail --silent --show-error --max-time 30 \
            https://sethsuwa-api.onrender.com/api/health
